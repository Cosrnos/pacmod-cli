module.exports = {
    // Basic
    PACKAGE_NAME: 'pacmod', // TODO: Default to package.json name if available
    MAIN_PACKAGE: 'main',

    // Build
    TEMP_FOLDER: '_build',

    // Dist
    BUILD_TARGET: 'dist',
    SCRIPT_PATH: '',

    // Serve
    PORT: 4000,

    // Testing
    TEST_PORT: 4001
};